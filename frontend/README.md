# Frontend

React + Vite + TypeScript operator application for Fraud-Spike Investigator.

```powershell
npm install
npm run dev
npm test
npm run build
```

Set `VITE_API_BASE_URL` (see `.env.example`). Default is `http://localhost:8000`.

See the root `README.md` for the full project setup.
